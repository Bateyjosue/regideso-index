// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

// Define your models and their relationships here

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_STRING")
}



model User {
  id            String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid()
  name          String
  email         String    @unique
  password_hash String
  role          Role      @default(USER)
}

enum Role {
  USER
  ADMIN
}
model Direction {
    num_direction String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid()
    code_direction String @unique
    name String
    Agencies Agency[]
    
    createdAt   DateTime @default(now())
    updatedAt   DateTime @updatedAt
}

model Agency {
    num_agency String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid()
    code_agency String @unique
    num_direction String @db.Uuid()
    name String
    direction Direction @relation(fields: [num_direction], references: [num_direction])

    createdAt   DateTime @default(now())
    updatedAt   DateTime @updatedAt
}

// model Avenue {
//     id    Int     @id @default(autoincrement())
// }

// model Level {
//     id    Int     @id @default(autoincrement())
// }

// model Category {
//     id    Int     @id @default(autoincrement())
// }

// model Agent {
//     id    Int     @id @default(autoincrement())
// }

// model Subscriber {
//     id    Int     @id @default(autoincrement())
// }